# OpenClaw Lightning Economic Layer — Python dependencies
#
# Install: pip install -r requirements.txt
# Python:  3.11+

# HTTP client — LNBits API calls
httpx==0.27.2

# Environment variable loading
python-dotenv==1.0.1

# Type checking support (stdlib in 3.11+ but pin for older envs)
# No additional deps needed — uses built-in typing module

# Development / testing
pytest==8.3.3
pytest-asyncio==0.23.8
respx==0.21.1          # Mock httpx requests in tests
